// enable validation for hidden fields
$.validator.setDefaults({
    ignore: []
});

// add jquery unobtrusive validation adapter
$.validator.unobtrusive.adapters.add('customrequired', [],
    function (options) {
        options.rules['customrequired'] = {
        };
        options.messages['customrequired'] = options.message;
    }
);

// add jquery unobtrusive validation adapter
$.validator.unobtrusive.adapters.add('customrequireda', [],
    function (options) {
        options.rules['customrequireda'] = {
        };
        options.messages['customrequireda'] = options.message;
    }
);

// add jquery unobtrusive validation adapter
$.validator.unobtrusive.adapters.add('customrequiredb', [],
    function (options) {
        options.rules['customrequiredb'] = {
        };
        options.messages['customrequiredb'] = options.message;
    }
);

// add jquery unobtrusive validation adapter
$.validator.unobtrusive.adapters.add('customrequiredc', [],
    function (options) {
        options.rules['customrequiredc'] = {
        };
        options.messages['customrequiredc'] = options.message;
    }
);

// add jquery unobtrusive validation adapter
$.validator.unobtrusive.adapters.add('customrequiredd', [],
    function (options) {
        options.rules['customrequiredd'] = {
        };
        options.messages['customrequiredd'] = options.message;
    }
);

// add jquery unobtrusive validation adapter
$.validator.unobtrusive.adapters.add('customrequirede', [],
    function (options) {
        options.rules['customrequirede'] = {
        };
        options.messages['customrequirede'] = options.message;
    }
);

// add jquery unobtrusive validation adapter
$.validator.unobtrusive.adapters.add('customrequired6', [],
    function (options) {
        options.rules['customrequired6'] = {
        };
        options.messages['customrequired6'] = options.message;
    }
);

// add jquery unobtrusive validation adapter
$.validator.unobtrusive.adapters.add('customrequired7', [],
    function (options) {
        options.rules['customrequired7'] = {
        };
        options.messages['customrequired7'] = options.message;
    }
);

// add jquery unobtrusive validation adapter
$.validator.unobtrusive.adapters.add('customrequired8', [],
    function (options) {
        options.rules['customrequired8'] = {
        };
        options.messages['customrequired8'] = options.message;
    }
);

// add jquery unobtrusive validation adapter
$.validator.unobtrusive.adapters.add('customrequired9', [],
    function (options) {
        options.rules['customrequired9'] = {
        };
        options.messages['customrequired9'] = options.message;
    }
);

// add jquery unobtrusive validation adapter
$.validator.unobtrusive.adapters.add('customrequired10', [],
    function (options) {
        options.rules['customrequired10'] = {
        };
        options.messages['customrequired10'] = options.message;
    }
);